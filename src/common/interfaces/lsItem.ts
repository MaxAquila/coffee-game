/**Definition of storage item. */
export interface lsItem {
    /**@readonly Key of the storage item. */
    readonly key: string;
    /**@readonly Default value of the storage item. */
    readonly value: any;
};